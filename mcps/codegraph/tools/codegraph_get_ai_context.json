{
  "name": "codegraph_get_ai_context",
  "description": "Gathers comprehensive code context optimized for AI understanding. USE WHEN: explaining code, planning modifications, debugging issues, or writing tests. THIS IS YOUR PRIMARY TOOL for understanding unfamiliar code. Returns: primaryContext (full source code), relatedSymbols (full source or signature-only when budget is tight), imports (file-level module imports), siblingFunctions (other functions in same file with signatures), dependencies, architecture (module, layer, neighbors), and debugHints (complexity, branches, exception handlers, early returns — debug intent only). Intent controls prioritization: 'explain' = dependencies + callers + siblings, 'modify' = tests + callers, 'debug' = call chain + debug hints, 'test' = example tests + mockable dependencies. maxTokens controls budget — high-priority sections get full source, remaining budget fills with signature-only symbols for maximum coverage. Requires uri and line parameters.",
  "inputSchema": {
    "properties": {
      "intent": {
        "default": "explain",
        "description": "What you plan to do with the context. Affects which related code is selected.",
        "enum": [
          "explain",
          "modify",
          "debug",
          "test"
        ],
        "type": "string"
      },
      "line": {
        "description": "Line number (0-indexed)",
        "type": "number"
      },
      "maxTokens": {
        "default": 4000.0,
        "description": "Maximum tokens of context to return",
        "type": "number"
      },
      "uri": {
        "description": "The file URI to get context for (e.g. file:///Users/me/project/src/main.rs)",
        "type": "string"
      }
    },
    "required": [
      "uri",
      "line"
    ],
    "type": "object"
  }
}