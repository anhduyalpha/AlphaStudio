{
  "name": "codegraph_traverse_graph",
  "description": "Advanced graph traversal for complex code exploration. USE WHEN: specialized analysis requiring custom traversal (not covered by get_callers/get_callees/get_dependency_graph). PREFER simpler tools for common cases. Returns nodes and edges discovered during traversal. edgeTypes filters which relationships to follow (e.g., ['calls', 'imports']). nodeTypes filters which node kinds appear in results. Identify start node via uri+line or startNodeId from symbol_search.",
  "inputSchema": {
    "properties": {
      "direction": {
        "default": "outgoing",
        "description": "Direction to traverse edges",
        "enum": [
          "outgoing",
          "incoming",
          "both"
        ],
        "type": "string"
      },
      "edgeTypes": {
        "description": "Types of edges to follow (e.g., ['calls', 'imports'])",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "limit": {
        "default": 100.0,
        "description": "Maximum number of nodes to return",
        "type": "number"
      },
      "line": {
        "description": "0-based line number of the symbol. Use with uri to identify the starting symbol.",
        "type": "number"
      },
      "maxDepth": {
        "default": 3.0,
        "description": "Maximum traversal depth",
        "type": "number"
      },
      "nodeTypes": {
        "description": "Filter results to specific node types",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "startNodeId": {
        "description": "Internal node ID from symbol_search. Alternative to uri+line.",
        "type": "string"
      },
      "summary": {
        "default": false,
        "description": "Return a condensed summary for large graphs",
        "type": "boolean"
      },
      "uri": {
        "description": "File URI (e.g. file:///Users/me/project/src/main.rs). Use with line to identify the starting symbol.",
        "type": "string"
      }
    },
    "type": "object"
  }
}