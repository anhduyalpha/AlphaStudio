{
  "name": "codegraph_memory_invalidate",
  "description": "Marks memory as outdated without deleting. USE WHEN: knowledge is superseded, bugs are fixed, decisions are reversed. Maintains history while preventing outdated info from surfacing.",
  "inputSchema": {
    "properties": {
      "id": {
        "description": "Memory ID to invalidate",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "type": "object"
  }
}