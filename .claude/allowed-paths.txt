# Write scope for the CURRENT unit. Overwritten by /next-unit step 3.
# One entry per line: exact file, directory prefix ending in '/', or glob
# ('*' = within one segment, '**' = across segments). '#' starts a comment.
#
# PROGRESS.md, this file, and logs/ are always writable (hardcoded in
# .claude/hooks/scope-guard.mjs). With no entries below, nothing else
# inside the repo is writable — the between-units state.

# Unit 7 (B4) — PLAN "Files:" list
src/protocol/uploads.ts
# Minimal acceptance-critical extension: completed sessions must be queryable
# for lost-finalize adoption without changing the legacy default listing.
src/api/client.js
server/src/routes/upload-sessions.ts
server/src/services/upload-session.ts
server/tests/
# client test location established by A1 (vitest include: src/**/*.test.*)
src/tests/
PROGRESS.md
evidence/
